@page "{gameId:guid}"
@model FeedbackModel
@{
    ViewData["Title"] = "Feedback";
}

<div class="bg-secondary bg-opcity-10 py-2 mb-5">
    <div class="container">
        <h1 class="display-4">@Model.Player, das ist dein erreichtes Resultat</h1>
    </div>
</div>

<div>
    <div class="container">
        <form method="post">
            <div class="mb-3">
                <h4>Points: <button class="btn btn-success" type="button">@Model.Points</button></h4>
                <h4>Target: <button class="btn btn-success" type="button">@Model.Target</button></h4>
            </div>

            <div class="mb-5">
                <h2>@Model.Message</h2>
            </div>

            <div class="mb-3">
                <a href="/problemlist" class="btn btn-primary" role="button">go on ...</a>
            </div>
        </form>
    </div>
</div>